# Recover MCAP Action

This action automatically inspects and recovers corrupted MCAP files using `mcap doctor` and `mcap recover`, then uploads the recovered file to Kleinkram and links it to the original file.
